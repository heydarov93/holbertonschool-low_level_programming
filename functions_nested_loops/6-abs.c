#include "main.h"

/**
 *_abs - computes the absolute value of an integer
 *
 *@n: int
 *
 *Return: absolute value of given parameter
  */

int _abs(int n)
{

	if (n < 0)
	{
		return (-(n));
	}

	return (n);
}
